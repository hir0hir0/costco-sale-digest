# 監査レポート（raw） — 2026-08-31

実行: `/review-site legal-risk`
対象: costco-sale-digest（commit a88ba28 時点）
このファイルは各レビュワーの出力を**無編集で**集約したもの。取捨選択は `2026-08-31-final.md` を見ること。

---

## legal-risk-reviewer

### [Critical] 購入店舗名・日単位の購入日・レシート合計金額が公開サイトにそのまま出ている
- **観点**: legal-risk-reviewer
- **対象**: `costco/build.py:117-123` / `costco/site_template.html:400-406` / `site/data.json`（`purchases`）/ `site/index.html`
- **影響ペルソナ・タスク**: 全員 / T8（掲載データの出所）、P0（運用者本人が被写体）
- **現状**: `build.py:117-123` が `purchases.json` の `date` / `store` / `total` を無加工で `site/data.json` に載せ、テンプレ 400-406 行が `<span class="rc-date">2026/08/29</span><span class="rc-store">◯◯倉庫店・14点</span><span class="rc-total">¥*****</span>` として描画する。`site/index.html` にも同じ値が埋め込み済み（`◯◯倉庫店` `*****` の出現を確認）。personas.md の公開ポリシー「粒度を落として公開」は 購入日→月単位／店舗→出さない／数量→出さない と定めているが、**3項目すべてから逸脱**している。さらに `items` は同一商品を明細行数ぶん重複して出す（`カルビ焼肉` が2行）ため、数量も復元できる。合計金額は価格情報ではなく1回の買い物の支出額で、公開ポリシーの「公開してよい」欄のどれにも該当しない。
- **なぜ問題か**: 「2026-08-29 に◯◯倉庫店で*****円ぶん買った」という記録は、personas.md が明示的に「出さずに済むものは出さない」とした行動記録そのもので、価格情報としての価値は金額部分だけで足りている。逸脱が公開URLとリポジトリの両方に出ている。
- **再現手順**: `python -c "import json;print(json.load(open('site/data.json',encoding='utf-8'))['purchases'][0].keys())"` → `date, items, store, total`。ブラウザで「購入履歴」タブを開くとカード見出しに日付・店舗・点数・合計が並ぶ。
- **修正案**: `build.py:117-123` の組み立てを allowlist に変える。(a) `"month": str(r.get("date",""))[:7]` だけを出し `date` を廃止、(b) `store` の行を削除、(c) `total` を出さない（月別合計は `site_template.html:374-384` が `r.total` に依存しているので、この集計ごと落とすか `items` の `effective` 合計を月単位でのみ出す）、(d) 同一 `item_no` の明細をマージして数量を潰す。テンプレ 400-406 は `${y}/${mo}` 表示に、`・${r.items.length}点` を削除。工数 **小**

### [Critical] レシート由来の日付が「前回購入」表示と価格履歴からも日単位で漏れる（購入履歴タブを直しても残る）
- **観点**: legal-risk-reviewer
- **対象**: `costco/store.py:211-226`（`last_purchases`）/ `store.py:229-242`（`price_stats`）/ `costco/site_template.html:336-337` / `costco_data/history.json` / `site/data.json`（`offers[].history`, `offers[].last_bought`）
- **影響ペルソナ・タスク**: 全員 / T2、T9
- **現状**: カードの meta 行が `前回購入 ¥1,578 (08/29)` を出す（`site_template.html:336-337`、`o.last_bought.on.slice(5)`）。さらに `history.json` にはレシート由来の点が `{"on":"2026-08-29","price":399,"source":"receipt"}` として日単位で入り、`price_stats` が `on` をそのまま `offers[].history` に流すため、`site/data.json` 内で `2026-08-29` が39箇所出現する。`costco_data/history.json` 自体も public リポジトリにコミットされている。
- **なぜ問題か**: 公開ポリシーの「購入日は月単位に丸める」は購入履歴タブだけの話ではない。前指摘（購入履歴タブ）を直しても、カード表示とグラフ用データに日単位の購入日が残り、店舗名を消しても「この日に来店した」は公開されたままになる。
- **再現手順**: `site/data.json` を `2026-08-29` で検索。`no:588141` の offer に `"last_bought":{"on":"2026-08-29","price":1578}` と `"history":[{"on":"2026-07-13",...},{"on":"2026-08-29","price":1998}]` が入っている。
- **修正案**: (a) `build.py:88` の `last_bought` を `{"month": lb["on"][:7], "price": lb["effective"]}` にし、テンプレ 337 行を `前回購入 ¥1,578 (2026/08)` にする。(b) `build.py` で `history` を組むときに `source=="receipt"` の点の `on` を月初日（`on[:7]+"-01"`）へ丸める。`price_stats` が `source` を落としているので、`store.py:237` に `"source": p.get("source")` を足してから build 側で丸めるのが最小変更。(c) 丸めた結果が同日に重なる場合は最新1点に寄せる。工数 **中**

### [Major] 公開フィルタが存在せず、purchases.json のキーが素通しになる設計（今後追加する項目も自動で公開される）
- **観点**: legal-risk-reviewer
- **対象**: `costco/build.py:98-123` / `CLAUDE.md:29-32` / `tests/test_costco.py`
- **現状**: `build.py` は明細（`items`）側だけ `item_no/name/price/coupon` を選び直しているが、レコード側は `r.get("date")` `r.get("store")` `r.get("total")` を直に読む。除外リストも allowlist もない。一方 `CLAUDE.md:29-32` が指示する取り込みスキーマは `{"date","store","total","items"}` で、公開ポリシー（personas.md）への言及がない。`tests/test_costco.py` に `purchases` の公開フィールドを固定する検査は無い（`build_site` のテストは 368-373 行の offer 1件のみ）。
- **なぜ問題か**: 現在の逸脱を手で直しても、次にレシートから支払方法・会員番号・レジ番号を拾って `purchases.json` に足した瞬間、同じ経路で無検査に公開される。公開ポリシーが「人間が毎回気をつける」前提のままで、仕組みで担保されていない。
- **再現手順**: `build.py:117-123` を読む。`purchases.append({...})` に渡るキーがレコード由来のものそのまま。`grep -n "purchase" tests/test_costco.py` → 0件。
- **修正案**: (a) `build.py` に `PUBLIC_RECEIPT_KEYS = ("month",)` / `PUBLIC_ITEM_KEYS = ("item_no","name","price","coupon","effective","now")` を定義し、これ以外は組み立て時に落とす（レコードは丸めた月だけを新規に作る形にする）。(b) `tests/` に「`purchases.json` に `store` / `member_no` / `payment` を入れて `site_data()` を呼び、出力 JSON 全文にそれらの値が現れないこと」を検査する回帰テストを追加（文字列検索1本で書ける）。(c) `CLAUDE.md:29-32` のスキーマ説明に「`store` はローカル用で公開されない」旨を追記する。工数 **小**

### [Major] public リポジトリの Git 履歴に店舗名入りのレシートが残っており、現ファイルの修正では消えない
- **観点**: legal-risk-reviewer
- **対象**: commit `1fb4203`（`costco_data/purchases.json` 追加）/ commit `40cd4b7`（`site/data.json`・`site/index.html` に反映）/ `README.md:70`
- **現状**: `curl https://api.github.com/repos/hir0hir0/costco-sale-digest` が `"private": false` を返す（Pages も 200）。`git log -S "◯◯" --all` → `40cd4b7` と `1fb4203` の2コミット。加えて `README.md:70` の記入例が実在の店舗名 `"store": "◯◯倉庫店"` と実際の日付・合計 `*****` をそのまま使っている。
- **なぜ問題か**: personas.md の公開ポリシーは「public にするなら Git 履歴も確認してから」と定めているが、確認前にコミットされた状態。上の3指摘を直しても、履歴と README の例から店舗・日付・合計は誰でも復元できる。
- **再現手順**: `git log --oneline -S "◯◯" --all` / `git show 1fb4203:costco_data/purchases.json`
- **修正案**: (a) まず `README.md:70` の例を `"store": "◯◯倉庫店"`・架空の日付と金額に差し替える（工数 **小**）。(b) 履歴は対象が2コミットと浅いので `git filter-repo --replace-text`（`◯◯倉庫店==>◯◯倉庫店`）で置換して force push、Actions を1回回して Pages を貼り直す（工数 **中**、Actions の履歴に残る過去アーティファクトは別途 retention に任せる）。(c) 履歴書き換えを避けるなら、店舗名の粒度だけでも許容できるかを決め、以後は指摘3の allowlist で再発を止める（工数 **小**）。

### [Major] `_probe.json` にメルマガ本文の逐語抜粋がコミットされており、README の「本文の複製は配布していません」と矛盾する
- **観点**: legal-risk-reviewer
- **対象**: `costco_data/_probe.json` / `costco/sources.py:557-571` / `.github/workflows/costco.yml:82-85,130-140` / `README.md:82-83`
- **現状**: `mail_probe` が各メールについて `"first_lines": lines[:20]`（本文冒頭20行）と、`--grep` 指定時は該当行の**前後±8行**（`sources.py:544-551`）を `_probe.json` に書く。現物には8通ぶんの `first_lines` 計約2,886文字と、grep 窓68行が入っている（`"メール内容が正しく表示されない場合は、こちらを クリックしてください。"` のような本文コピーを含む）。ワークフローは `git add costco_data site costco_sources.json` で `costco_data` を丸ごとコミットするため、mailprobe を回すたびに本文断片が public リポジトリに増える。README:82-83 は「商品名・価格などの事実情報と、コストコCDNへの画像リンクのみを扱い、メルマガ本文や画像の複製は配布していません」と書いている。
- **なぜ問題か**: 抽出済みの価格・商品名・商品番号は事実情報だが、`first_lines` と grep 窓は本文の逐語コピーで、事実の抽出ではない。デバッグ用の副産物が公開物になっており、README の明示的な記述が現物と食い違っている。
- **再現手順**: `python -c "import json;d=json.load(open('costco_data/_probe.json',encoding='utf-8'));print(d[0]['first_lines'])"`
- **修正案**: (a) `_probe.json` を `.gitignore` に足し、workflow の `git add` から外して `upload-artifact`（短期 retention）に切り替える（工数 **小**）。(b) それでも `git pull` で読みたい運用（CLAUDE.md:22-24）を保つなら、`sources.py:563` の `first_lines` を廃止して `"first_lines_len": [len(l) for l in lines[:20]]` 等のメタ情報にし、`price_line_groups` は `find_prices` が当たった行の**数値部分と行番号だけ**に切り詰める（工数 **中**）。(c) 現行 `_probe.json` は中身を空配列にして再コミットする。工数 **小**

### [Major] mailprobe を実行した run のアーティファクトに、メルマガの生HTML全文が public リポジトリの成果物として公開される
- **観点**: legal-risk-reviewer
- **対象**: `.github/workflows/costco.yml:120-128`（`取得した生HTMLを保存` / `if: always()`）/ `costco/sources.py:181-187`（`capture`）/ `sources.py:534-536`
- **現状**: `mail_probe` は `capture(page, "mail-" + subject[:40])` でメール本文HTMLをそのまま `captures/costco/*.html` に書き出す（`sources.py:187` は `page.html` を無加工で保存）。workflow はモードに関係なく `if: always()` で `captures/costco/` を artifact `captures` としてアップロードし、retention 14日。リポジトリは public。なお通常の `update` 経路（`collect_mail`, `sources.py:498-516`）は capture を呼ばないので、生HTMLが出るのは mailprobe / probe / discover を回した run に限られる。
- **なぜ問題か**: 抽出結果ではなく配信物そのもの（HTML全文・レイアウト・コピー）が公開ダウンロード可能になる。加えてマーケティングメールのリンクやピクセルには購読者識別子が埋まっていることが多く、その場合は受信者の識別情報も一緒に出る（`captures/` はローカルに存在せず中身は**未確認**）。
- **再現手順**: workflow を `mode: mailprobe` で実行 → run ページの Artifacts に `captures` が出る。コードは `sources.py:536` → `capture()` → `costco.yml:120-128` の順に読む。
- **修正案**: (a) アップロードの条件を `if: always() && (inputs.mode == 'probe' || inputs.mode == 'discover')` にして、メール由来の capture を成果物から外す（工数 **小**）。(b) `mail_probe` 側で capture の保存先を `captures/mail/` に分け、artifact のパスを `captures/costco/`（Web由来）だけにする（工数 **小**）。(c) メール本文は保存せずメモリ内だけで扱い、必要なら `html_bytes` と行数のみ記録する（工数 **中**）。(d) 併せて `retention-days: 14` を `1` に下げる。

### [Minor] 商品画像410件をコストコCDNから直接埋め込み、`referrerpolicy="no-referrer"` で先方の遮断判断を無効化している
- **観点**: legal-risk-reviewer
- **対象**: `costco/site_template.html:344-346` / `costco/build.py:9-14,47-52` / `costco_sources.json`（`"images": "link"`）
- **現状**: 公開データ432件中410件が `https://www.costco.co.jp/medias/sys_master/images/...webp` を `<img src>` に持つ。`build.py` の docstring は「リポジトリに画像を溜めず、複製もしない。先方が消せばその時点で壊れる」を根拠にしているが、テンプレは `referrerpolicy="no-referrer"` を付けており、リクエストに参照元が乗らない。`alt=""` で出典表記もない。
- **なぜ問題か**: 複製していないのは事実だが、no-referrer によって配信元は「どこから読まれているか」を把握できず、hotlink を検知して止めるという自己制御の前提が実際には成り立たない。画像は商品名・価格と違い事実情報ではない。
- **再現手順**: `site_template.html:344-346` を読む。`site/data.json` の `image_src` のホストは全件 `www.costco.co.jp`。
- **修正案**: (a) `referrerpolicy="no-referrer"` を外し、先方が参照元を見て遮断できる状態に戻す（工数 **小**）。(b) 画像の必要性が低いなら `costco_sources.json` の `images` を `"off"` にする（`build.py:47-52` が対応済みなのでデータ再生成だけ、工数 **小**）。(c) 画像を残すなら、カード内かフッタに「商品写真は costco.co.jp のものを参照表示しています」と出典を明示する（工数 **小**）。

### [Minor] 非公式である旨と価格の免責が footer のみで、印刷出力からは消える
- **観点**: legal-risk-reviewer
- **対象**: `costco/site_template.html:141,186,193-199,6-7`
- **現状**: 免責文（`価格・期間は…誤りが含まれることがあります` `当ページはコストコホールセールジャパン株式会社とは無関係の個人サイトです`）は `<p class="noprint">` に入っており、`@media print` の 141 行 `.controls,.stats,.themebtn,.add,.spark,footer .noprint{ display:none !important; }` で印刷時に消える。買い物リストには印刷ボタン（186行）がある。`<title>`（7行）と `meta description`（6行）は「コストコ 安売りまとめ」で、非公式の語はない。
- **なぜ問題か**: 印刷して店頭に持ち込む紙面には、コストコ名と商品名・価格だけが残り、非公式である旨も価格の正確性の免責も付かない。検索結果やタブに出るタイトルにも非公式表示がない。
- **再現手順**: サイトで「買い物リスト」→「印刷」→ プレビューのフッタを見る。CSS は 141 行。
- **修正案**: (a) 免責 `<p>` を2つに割り、「非公式・価格は要確認」の1文だけ `noprint` を外す（工数 **小**）。(b) 印刷用に `@media print` で1行の短い注記を出す（工数 **小**）。(c) `build.py:29` の `SITE_TITLE` を「コストコ 安売りまとめ（非公式）」にし、`meta description` にも同旨を入れる（工数 **小**）。

## サマリ（legal-risk-reviewer）
- Critical: 2件 / Major: 4件 / Minor: 2件 / Idea: 0件
- **もし1つだけ直すなら**: `build.py:117-123` を allowlist 化して、購入店舗・日単位の購入日・レシート合計・明細点数を公開データから落とす（Critical 1件目と Major 3件目を同時に塞げる）
- **確認できなかった範囲**:
  - `captures/` の実物（ローカルに存在せず、Actions アーティファクトにも未アクセス）。メルマガHTML内に購読者識別子つきURLが含まれるかは未確認。
  - 過去の Actions run に残るアーティファクトとログの実際の公開状態（`gh auth` 未設定のため API で確認できず）。
  - 取得方法については逸脱を確認できなかった: `costco_sources.json` の `web` は空でWeb巡回は稼働しておらず、`sources.py:152-172,296-310` が robots.txt を読んで不許可URLを除外し、`Robots.delay` を待つ実装になっている。メールは IMAP の読み取り専用で自分宛のもの。この項目は現状で公開ポリシーに反していない。
  - 転載範囲については、`offers[].name` は商品名、`note` は全件空、`source_label` はメール件名のみで、本文の説明文・キャッチコピーが画面に出ている箇所は無かった（本文の逐語コピーが出るのは上記 `_probe.json` と `captures/`）。
- **CLAUDE.md に追記すべき知見**:
  - 「レシート取り込み」節のスキーマ（29-32行）に、`store` と日単位の `date` は**ローカル保持のみで公開データには出さない**こと、公開データの粒度は personas.md の公開ポリシー表に従うことを明記する。現行の 40-41 行「⚠️ purchases.json は公開される」は事実として正しいが、ポリシーとの整合が書かれていない。
  - `costco_data/` は workflow が丸ごと `git add` する＝**このディレクトリに置いたものは公開される**ことを明記する（`_probe.json` のような調査用ファイルを気軽に置けない）。
