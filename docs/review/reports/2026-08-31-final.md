# レビュー最終版 2026-08-31

対象: costco-sale-digest（commit a88ba28 時点）
入力: `docs/review/reports/2026-08-31-raw.md`（legal-risk-reviewer / 8件）
役割: 反証・裁定。各指摘の根拠ファイルを再度自分で開いて検証した。

---

## 今すぐ直すべき3件

1. **`costco/build.py:117-123` と `88` の公開データから、購入店舗名・日単位の購入日・レシート合計・明細点数を落とす**
   （対象: `costco/build.py` / `costco/site_template.html:337,378-382,403-405` / `costco_data/purchases.json` / `costco_data/history.json`）
2. **`README.md:70` の記入例を、実在の店舗名・購入日・合計金額から架空値に差し替える**
   （現在 `"store": "◯◯倉庫店"` / `2026-08-29` / `*****` が公開中。1行の修正で済む）
3. **`.github/workflows/costco.yml:120-128` の `captures` アーティファクト upload を、メール由来のキャプチャから外す**
   （2026-08-26 の mailprobe run でメルマガ生HTML 8通分がアップロード済み。retention 14日＝**現時点でまだ公開ダウンロード可能**）

---

## 維持された指摘（深刻度順）

### [Critical] レシート由来データが公開ポリシーの粒度を超えて、5つの公開物すべてに日単位・店舗名つきで出ている
- **観点**: legal-risk-reviewer（raw の Critical 1件目・2件目を**統合**）
- **対象**:
  - `costco/build.py:117-123`（purchases 組み立て）/ `costco/build.py:88`（`last_bought`）
  - `costco/site_template.html:337`（前回購入）/ `378-382`（月別合計）/ `403-405`（レシートカード見出し）
  - `costco/store.py:196-207`（`merge_receipts`）/ `229-242`（`price_stats`）
  - 公開物: `site/data.json` / `site/index.html` / `costco_data/purchases.json` / `costco_data/history.json` / `README.md:70`
- **影響ペルソナ・タスク**: 全員 / T8、P0（運用者本人が被写体）
- **現状**（自分で再現・全項目確認済み）:
  - `site/data.json` の `purchases[0]` のキーは `date, items, store, total`。値は `date=2026-08-29` / `store=◯◯倉庫店` / `total=*****` / `items` 14行。`site/index.html` にも同じ値が埋め込み済み（`◯◯` の出現 1件）。
  - `items` は同一 `item_no` を明細行数ぶん重複して持つ（`90223 カルビ焼肉` が 2行）ため、数量が復元できる。テンプレ 404行は `・${r.items.length}点` として点数を直接表示する。
  - `offers[].last_bought` は `{"on":"2026-08-29","price":1578}`、`offers[].history` にも `{"on":"2026-08-29","price":1998}` が入る。`site/data.json` 内の `2026-08-29` の出現は **39箇所**。
  - `costco_data/history.json` にはレシート由来の点が `{"on":"2026-08-29","price":399,"source":"receipt"}` の形で **13点**、日単位のまま public リポジトリにコミットされている。
  - `README.md:70` の記入例が実在の `"store": "◯◯倉庫店"` / `2026-08-29` / `*****` をそのまま使っている。
  - personas.md 公開ポリシー「粒度を落として公開」は 購入日→月単位／購入店舗→出さない／購入数量→出さない と定めており、**3項目すべてから逸脱**。レシート合計はどの許可欄にも該当しない。
- **なぜ問題か**: 「2026-08-29 に◯◯倉庫店で*****円ぶん買った」は personas.md が「出さずに済むものは出さない」とした行動記録そのもので、価格情報としての価値は金額だけで足りている。購入履歴タブだけを直しても、カードの `前回購入 (08/29)` とグラフ用 `history`、および `costco_data/*.json` 本体に日付が残る。
- **再現手順**:
  `python -c "import json;d=json.load(open('site/data.json',encoding='utf-8'));print(d['purchases'][0].keys())"` → `date, items, store, total`
  `grep -o "2026-08-29" site/data.json | wc -l` → 39
  `python -c "import json;d=json.load(open('costco_data/history.json',encoding='utf-8'));print(sum(1 for v in d.values() for p in v['points'] if p.get('source')=='receipt'))"` → 13
- **修正案**（raw の案を一部差し替え。理由は反証メモ）:
  - (a) **取り込み時に落とす（推奨・根治）**: `store` は `purchases.json` に保存しない。personas.md「店舗別の価格比較機能は不要」より下流の用途が無く、保持する理由がない。CLAUDE.md のスキーマも合わせて直す。工数 **小**
  - (b) `store.py:196-207` の `merge_receipts` が `_record_price` に渡す日付を月初（`YYYY-MM-01`）に丸める。`history.json` 自体が公開物なので、build 側だけで丸めても解決しない。工数 **小**
  - (c) `build.py:117-123` を `{"month": date[:7], "items": [...]}` に変え、`date` / `store` / レコード単位の `total` を出さない。同一 `item_no` はマージして数量を潰す。工数 **小**
  - (d) `build.py:88` を `{"month": lb["on"][:7], "price": lb["effective"]}` に、テンプレ 337行を `前回購入 ¥1,578 (2026/08)` に。工数 **小**
  - (e) 月別合計（テンプレ 378-382）は**残す**。月単位の集計は公開ポリシーの粒度に収まり、T10（取り込みが正しく終わったかの確認）に効く。レコード単位の `total` を消し、`items[].effective` の月合計に置き換える。工数 **小**
  - (f) `README.md:70` の例を架空値に差し替える。工数 **小**
- **反証メモ**: raw は「合計金額は公開ポリシーのどの欄にも該当しない」から `site_template.html:374-384` の月別集計ごと落とすことを選択肢に挙げたが、**月別合計まで消すのは過剰**。ポリシーが禁じているのは日単位・店舗単位の粒度であり、月合計は許容範囲かつ運用者の検算導線になる。また raw の修正案 (b)「build 側で history を丸める」は `costco_data/history.json` 自身が public リポジトリにコミットされる正本であることを見落としており、**それだけでは日付が消えない**（`merge_receipts` の入口で丸める必要がある）。この2点を差し替えたうえで指摘本体は維持する。

### [Major] 公開フィルタ（allowlist）が無く、`costco_data/` に置いたものが無検査で公開される構造
- **観点**: legal-risk-reviewer（raw の Major 1件目。`_probe.json` 指摘の構造部分を**統合**）
- **対象**: `costco/build.py:98-123` / `.github/workflows/costco.yml:128-133` / `tests/test_costco.py` / `CLAUDE.md:29-32`
- **影響ペルソナ・タスク**: P0 / 公開ポリシー全般
- **現状**: `build.py` は `items` 側だけ `item_no/name/price/coupon` を選び直しているが、レコード側は `r.get("date")` `r.get("store")` `r.get("total")` を直読みしており、allowlist も除外リストも無い。ワークフローの commit ステップは `git add costco_data site costco_sources.json` で **`costco_data/` を丸ごとコミット**する。`tests/test_costco.py` に `purchases` の公開フィールドを固定する検査は無い（`purchase` の出現は 554/567/568 行のフィクスチャ定義のみで、公開面の assert は 0件）。
- **なぜ問題か**: 上の Critical を手で直しても、次にレシートから支払方法・会員番号・レジ番号を拾って `purchases.json` に足した瞬間、同じ経路で無検査に公開される。公開ポリシーが「人間が毎回気をつける」前提のままで、仕組みで担保されていない。同じ構造で `_probe.json` のような調査用ファイルも自動的に公開物になっている。
- **再現手順**: `build.py:117-123` を読む → `purchases.append({...})` に渡るキーがレコード由来のまま。`grep -n "purchase" tests/test_costco.py` → フィクスチャ3行のみ。
- **修正案**: (a) `build.py` に `PUBLIC_RECEIPT_KEYS = ("month","items")` / `PUBLIC_ITEM_KEYS = ("item_no","name","price","coupon","effective","now")` を定義し、これ以外は組み立て時に落とす。(b) `tests/` に「`purchases.json` に `store` / `member_no` / `payment` を入れて `site_data()` を呼び、出力 JSON 全文にそれらの値が現れないこと」を検査する回帰テストを追加（文字列検索1本で書ける）。(c) `costco_data/` は workflow が丸ごと `git add` する＝置いたものは公開される、を CLAUDE.md に明記。工数 **小**
- **反証メモ**: raw では別々の3指摘（allowlist 不在／`_probe.json`／`captures`）に分かれていたが、`git add costco_data` という単一の原因を共有するため、再発防止のテストとしては1件に統合するのが正しい。指摘の事実関係はすべて自分で確認できた。

### [Major] mailprobe の Actions アーティファクトにメルマガ生HTMLが入り、public リポジトリの成果物として**現在も**ダウンロード可能
- **観点**: legal-risk-reviewer
- **対象**: `.github/workflows/costco.yml:120-128`（`取得した生HTMLを保存` / `if: always()` / `retention-days: 14`）/ `costco/sources.py:44,181-187,536`
- **影響ペルソナ・タスク**: 全員 / 公開ポリシー（レシート原本に準じる「原本を出さない」原則）
- **現状**: `CAPTURE_DIR = ROOT / "captures" / "costco"`（`sources.py:44`）。`mail_probe` は `sources.py:536` で `capture(page, "mail-" + subject[:40])` を呼び、`capture()` は `page.html` を無加工でこのディレクトリに書く（`sources.py:187`）。ワークフローはモードに関係なく `if: always()` で `captures/costco/` を artifact `captures` としてアップロードする。リポジトリは public（`api.github.com/repos/hir0hir0/costco-sale-digest` → `"private": false`、Pages 200 を実測）。
  **raw が「未確認」とした点を確定させた**: `costco_data/_probe.json` の `capture` フィールドが全8件 `/home/runner/work/costco-sale-digest/costco-sale-digest/captures/costco/20260826-0211xx-mail-*.html` を指しており、**mailprobe は 2026-08-26 に GitHub Actions 上で実際に実行されている**。retention 14日のため、その run の `captures` アーティファクト（メルマガ生HTML 8通分）は 2026-09-09 頃まで公開ダウンロード可能な状態にある。
  なお通常の `update` 経路（`collect_mail`）は `capture` を呼ばないので、生HTMLが出るのは mailprobe / probe / discover を回した run に限られる。`captures/` は `.gitignore` 済みでリポジトリ本体には入っていない。
- **なぜ問題か**: 抽出結果ではなく配信物そのもの（HTML全文・レイアウト・コピー）が公開ダウンロード可能になる。README:82-83 の「メルマガ本文や画像の複製は配布していません」とも矛盾する。
- **再現手順**: `python -c "import json;d=json.load(open('costco_data/_probe.json',encoding='utf-8'));print([e['capture'] for e in d])"` → runner の絶対パス。該当 run ページの Artifacts に `captures` が残る。
- **修正案**: (a) `mail_probe` 側の capture 保存先を `captures/mail/` に分け、artifact の `path` は `captures/costco/`（Web由来）だけにする。工数 **小**（最小変更で確実）
  (b) 併せて `retention-days` を `14` → `1` に下げる。工数 **小**
  (c) 2026-08-26 の run のアーティファクトを手動で削除する（期限切れを待つより早い）。工数 **小**
- **反証メモ**: raw の「マーケティングメールのリンクやピクセルには購読者識別子が埋まっていることが多く、受信者の識別情報も一緒に出る」は**未確認の推測なので根拠から外した**（raw 自身も未確認と書いている）。この推測が無くても、配信物の全文が公開されている事実と README との矛盾だけで Major は成立する。優先度を上げたのは推測の重大さではなく、**公開期限が切れる前に消せる**という時間的理由。

### [Major] public リポジトリの Git 履歴に店舗名入りのレシートが残り、現ファイルの修正では消えない
- **観点**: legal-risk-reviewer
- **対象**: commit `1fb4203`（`costco_data/purchases.json` 追加）/ commit `40cd4b7`（`site/data.json`・`site/index.html` に反映）
- **影響ペルソナ・タスク**: 全員 / 公開ポリシー「public にするなら Git履歴も確認してから」
- **現状**: `git log --oneline -S "◯◯" --all` → `40cd4b7` / `1fb4203` の2コミット（実測）。`git remote -v` は `https://github.com/hir0hir0/costco-sale-digest.git`、API は `"private": false`。
- **なぜ問題か**: personas.md の公開ポリシーは Git 履歴の確認を明示的に条件にしているが、確認前にコミットされた状態。上の Critical を直しても履歴から店舗・日付・合計を復元できる。
- **再現手順**: `git log --oneline -S "◯◯" --all` / `git show 1fb4203:costco_data/purchases.json`
- **修正案**: (a) まず `README.md:70` の例を差し替える（Critical (f) と同じ。**これは履歴ではなく現行ファイルなので最優先**）。工数 **小**
  (b) 履歴は対象が2コミットと浅く、全コミットが自分と bot のものなので `git filter-repo --replace-text`（`◯◯倉庫店==>◯◯倉庫店`）で置換して force push、Actions を1回回して Pages を貼り直す。工数 **中**
  (c) 履歴書き換えを避けるなら、**店舗名の粒度だけは許容する**と決めて記録に残し、以後は allowlist（Major 1件目）で再発を止める。工数 **小**
- **反証メモ**: 事実関係は全て再現できた。ただし「履歴を書き換えるべき」は必須ではない。(c) の「許容すると決める」も personas.md の趣旨（判断を明示的に残す）に合致する正当な結論であり、force push を Major の必須対応として扱わない。**必須なのは (a) の README だけ**。

### [Minor] `_probe.json` にメルマガの定型文が逐語で残り、README の「本文の複製は配布していません」と厳密には矛盾する
- **観点**: legal-risk-reviewer（raw では Major。**降格**）
- **対象**: `costco_data/_probe.json` / `costco/sources.py:544-551,563-566` / `README.md:82-83`
- **影響ペルソナ・タスク**: 全員 / 公開ポリシー（転載）
- **現状**: `mail_probe` は各メールについて `"first_lines": lines[:20]` と、`--grep` 指定時は該当行の前後±8行を `_probe.json` に書く。現物は8通分で `first_lines` 計 2,886文字、grep 窓 68行（実測）。中身を読むと**大半は商品名・ITEM番号・価格**（例: `"263: BRAUN 光美容器 シルクエキスパート"` `"265: ITEM #62471"` `"271: ¥38,680"`）で、これは README が「事実情報」として明示的に許容している内容そのもの。逐語の非事実文は `"メールが正しく表示されない方は、こちら をご覧ください。"` `"Members' Favorites"` `"あなたの暮らしに、新しいお気に入りを。"` 等の定型文・惹句が数文あるだけ。
- **なぜ問題か**: 量は僅かだが README の記述は「複製は配布していません」と無条件で、現物と厳密には食い違う。
- **修正案**: (a) `sources.py:563` の `first_lines` を落とし、`price_line_groups` は数値と行番号だけに切り詰める。工数 **中** (b) 現行 `_probe.json` を空配列にして再コミット。工数 **小** (c) 手を入れないなら README:82-83 に「調査用ファイル `_probe.json` には抽出元の行断片が含まれます」と1文足して記述を実態に合わせる。工数 **小**
- **反証メモ**: raw は Major としたが**降格**。根拠は、`_probe.json` の中身を実際に開いた結果、reviewer の言う「本文の逐語抜粋」の実体が**ほぼ商品名・価格・商品番号＝README が許容する事実情報**であり、創作的表現の複製と呼べるのは定型文数文にとどまるため。また raw が現物からの引用として挙げた `"メール内容が正しく表示されない場合は、こちらを クリックしてください。"` は**実ファイルに存在しない文字列**（実際は `"メールが正しく表示されない方は、こちら をご覧ください。"`）で、引用が不正確だった。指摘の構造的な部分（`costco_data/` は丸ごと公開される）は Major 1件目に統合済み。

### [Minor] 非公式である旨と価格の免責が印刷出力から消える
- **観点**: legal-risk-reviewer
- **対象**: `costco/site_template.html:141,186,195-199` / `costco/build.py:29`
- **影響ペルソナ・タスク**: P1 / T1（買い物リストを印刷して店頭へ持ち込む導線）
- **現状**: 免責文は `<p class="noprint">`（195-199行）に入っており、`@media print` の 141行 `.controls,.stats,.themebtn,.add,.spark,footer .noprint{ display:none !important; }` で印刷時に消える。買い物リストには印刷ボタンがある（186行、`site_template.html:488` の `window.print()`）。`<title>`（7行）は「コストコ 安売りまとめ」で非公式の語がない。
- **なぜ問題か**: 印刷した紙面にはコストコ名と商品名・価格だけが残り、非公式である旨も価格の正確性の免責も付かない。
- **再現手順**: 「買い物リスト」→「印刷」→ プレビューのフッタ。CSS は 141行。
- **修正案**: (a) 免責 `<p>` を2つに割り、「非公式・価格は要確認」の1文だけ `noprint` を外す。工数 **小** (b) `build.py:29` の `SITE_TITLE` を「コストコ 安売りまとめ（非公式）」にする。工数 **小**
- **反証メモ**: CSS・HTML・印刷ボタンの導線を実際に追って再現できた。raw は `meta description`（6行）も「コストコ 安売りまとめ」と引用しているが、実際は `"コストコのセール・クーポン・値下がり情報をまとめたページ"` で引用が不正確。ただし「非公式の語が無い」という結論は変わらないため指摘は維持する。

---

## 棄却・降格した指摘

| 元の指摘 | 判定 | 理由 |
|---|---|---|
| [Critical] 購入店舗名・日単位の購入日・レシート合計が公開されている | **統合** | 下の指摘と原因（build.py に公開粒度フィルタが無い）が同一。1件の Critical にまとめた |
| [Critical] レシート日付が前回購入表示と価格履歴からも漏れる | **統合** | 同上。ただし「購入履歴タブを直しても残る」という指摘の芯は統合後の修正案 (b)(d) に保存した |
| 修正案「月別合計（テンプレ 374-384）ごと落とす」 | **棄却（案のみ）** | 月単位の集計は公開ポリシーの粒度内で、T10 の検算導線として有用。粒度違反は日単位・店舗名であって月合計ではない |
| 修正案「build 側で history の日付を丸める」 | **棄却（案のみ）** | `costco_data/history.json` 自体が public リポジトリの正本なので、build 側だけ丸めても日付は公開されたまま。`merge_receipts` の入口で丸める必要がある |
| [Major] `_probe.json` にメルマガ本文の逐語抜粋 | **降格 → Minor** | 現物を開くと中身の大半が商品名・ITEM番号・価格＝README が許容する事実情報で、逐語の非事実文は定型文数文にとどまる |
| 同指摘の引用 `"メール内容が正しく表示されない場合は…"` | **棄却（根拠のみ）** | 実ファイルに存在しない文字列。実際は `"メールが正しく表示されない方は、こちら をご覧ください。"` |
| [Major] captures 内のメルマガに購読者識別子が埋まっている可能性 | **棄却（根拠のみ）** | raw 自身が「未確認」と書いた推測。指摘本体は生HTML公開の事実だけで成立するため、推測を根拠から外して維持した |
| [Major] Git 履歴の書き換え（force push）を必須対応とする | **降格（対応範囲のみ）** | personas.md が求めるのは「履歴の確認」であって書き換えではない。必須なのは現行ファイルである README:70 の修正のみで、履歴は「許容すると決める」も正当な結論 |
| [Minor] `referrerpolicy="no-referrer"` が先方の hotlink 遮断判断を無効化 | **降格 → Idea（提案レーンへ）** | 画像を複製していないのは事実でチェックリスト H の「転載」項目は満たしており、現状壊れていない。加えて修正案 (a)（属性を外す）は多くの CDN の referer 制限で画像表示を壊すおそれがあり、欠陥の修正ではなく方針の選択にあたる |
| [Minor] 印刷免責の `meta description` 引用 | **棄却（根拠のみ）** | 引用が不正確（実際は「コストコのセール・クーポン・値下がり情報をまとめたページ」）。「非公式の語が無い」という結論は成立するため指摘は維持 |

**棄却対象にならなかった検証項目**（raw の判断を追認）: 取得方法（`costco_sources.json` の `web` は空、`sources.py:152-175` が robots.txt を読み `Robots.delay` を待つ、メールは IMAP 読み取り専用）は公開ポリシーに反していない。`offers[].note` は全432件が空で、画面上にメルマガの説明文・キャッチコピーが出る箇所は無い。`captures/` は `.gitignore` 済みでリポジトリ本体には入っていない。

---

## CLAUDE.md への追記提案

```diff
 ## レシート取り込み（ユーザーが写真を貼って「レシート取り込んで」と言ってくる）

 1. レシート画像から明細を読み取り、`costco_data/purchases.json` の配列に追記する:
-   {"date": "YYYY-MM-DD", "store": "◯◯倉庫店", "total": *****,
+   {"date": "YYYY-MM-DD", "total": *****,
     "items": [{"item_no": "588141", "name": "…", "price": 1998, "coupon": 420}]}
+   - **店舗名は保存しない**。personas.md の公開ポリシーで「出さない」区分であり、
+     「店舗別の価格比較機能は不要」なので下流の用途も無い
+   - `date` はローカル保持。**公開データ（site/・history.json）には月単位でしか出さない**
```

```diff
-⚠️ purchases.json は公開される（買った物と日付が見える）。ユーザーが嫌がったら
-空配列にして backfill で履歴を作り直す。
+⚠️ `costco_data/` は workflow が `git add costco_data site costco_sources.json` で
+**丸ごとコミットする**。このディレクトリに置いたファイルは全部 public になる
+（調査用の `_probe.json` も例外ではない）。公開してよい粒度は personas.md の
+「公開ポリシー」表が正典で、CLAUDE.md ではなくそちらを見ること。
+ユーザーが嫌がったら purchases.json を空配列にして backfill で履歴を作り直す。
```

```diff
 ## 落とし穴（全部実際に踏んだ・tests/ に回帰テストあり）
+
+- **公開粒度は build.py だけでは守れない**。`history.json` と `purchases.json` は
+  それ自体が public リポジトリの正本なので、build 時に丸めても元データに日付が残る。
+  粒度を落とすなら `store.merge_receipts` の入口（取り込み時）で落とすこと
+- **mailprobe / probe / discover を Actions で回すと、`captures/` が
+  アーティファクトとして公開される**（`.gitignore` してあるのはリポジトリ側だけ）。
+  メルマガ生HTMLを出したくないなら workflow の upload path を絞る
```

---

## 提案レーンへ送る

| 内容 | 元の指摘者 |
|---|---|
| 商品画像の扱いを方針として決める: `referrerpolicy="no-referrer"` を外して先方が参照元を見て遮断できる状態に戻すか、`costco_sources.json` の `images` を `"off"` にするか、画像を残したうえでフッタに「商品写真は costco.co.jp のものを参照表示しています」と出典を明示するか。属性を外すと CDN 側の referer 制限で 410件の画像が表示されなくなる可能性があるため、先に1件で実測してから決める | legal-risk-reviewer |
| `SITE_TITLE` と `meta description` に「非公式」を入れて、検索結果・タブ・OGP でも公式と誤認されないようにする（印刷免責の Minor とは別に、公開フェーズの見せ方の話） | legal-risk-reviewer |
| `_probe.json` を「公開しても困らない要約」に設計し直す（行断片ではなく、行番号・文字数・抽出できた件数だけを持つ形）。`git pull` で調査結果を読める運用は維持したまま公開面の心配を消せる | legal-risk-reviewer |

---

## 統計

- 入力 **8件** → 維持 **6件**（うち統合により 2件→1件、実ブロック数 **6**）/ 降格 **2件**（Major→Minor 1、Minor→Idea 1）/ 棄却 **0件**（ただし個別の根拠・修正案レベルで **5点**を棄却）/ 提案へ送付 **3件**
- 確定した深刻度: **Critical 1件 / Major 3件 / Minor 2件**
- 最も指摘が集中した領域: **`costco/build.py` の公開データ組み立て（`git add costco_data` を含む「公開面のフィルタ不在」という単一の原因に、8件中5件が帰着した）**
- レビュワーの精度: 事実関係は概ね正確で、行番号の引用はすべて実物と一致した。一方で**引用文の再現に3箇所の不正確**（`_probe.json` の定型文、`meta description`、および修正案が `history.json` の公開性を見落とし）があり、根拠の孫引きには注意が必要。

---

**今回のレビューで、直さなくてよいと判断できたことは何か。**
サイトを非公開に戻すこと、Git 履歴を force push で書き換えること、月別合計の表示を消すこと、商品画像のリンク参照をやめること、そして `_probe.json` を「メルマガ本文の転載」として扱うこと — この5つは今回直さなくてよい。実際に直すべきは、`build.py` と `merge_receipts` で公開粒度を落とすこと、`README.md:70` の実データ例を差し替えること、mailprobe のアーティファクトからメルマガ生HTMLを外すこと、の3つに尽きる。
